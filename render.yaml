services:
  - type: web
    name: flask-seo-ai
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PORT
        value: 10000
      - key: OPENAI_API_KEY
        sync: false
